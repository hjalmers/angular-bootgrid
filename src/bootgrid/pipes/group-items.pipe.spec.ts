import { GroupItemsPipe } from './group-items.pipe';

describe('GroupItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
