import { GroupWidthDirective } from './group-width.directive';

describe('GroupWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupWidthDirective();
    expect(directive).toBeTruthy();
  });
});
